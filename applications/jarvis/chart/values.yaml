replicaCount: 1

image:
  repository: thedarkest22/ai-agent  # Docker Hub
  tag: "latest"
  pullPolicy: Always

service:
  type: ClusterIP
  port: 3000

ingress:
  enabled: true
  className: nginx
  paths:
    - path: /jarvis
